#include<iostream>
#include<array>
using namespace std;

int main()
{
    // https://leetcode.com/problems/maximum-subarray/submissions/1321615969/
    /*int sum=0;
    
    int n;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    int max=a[0];
    //int a[9]={-2,1,-3,4,-1,2,1,-5,4};
    for(int i=0;i<n;i++)
    {
        sum+=a[i];
        if(max<sum)
        {
            max=sum;
        }
        if(sum<0)
        {
            sum=0;
        }
    }
    cout<<max;

    int maxSubArray(vector<int>& nums) 
    {
       int maxx=INT_MIN;
       int sum=0;
    for(const int num:nums )
    {
        sum=max(num,sum+num);
        maxx=max(maxx,sum);
    }
    return maxx; 
    }
*/


    
    /*int n;
    cin>>n;
    int b[n];    for user-defined size of the array
    
    
    int n;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    for(int i=0;i<n;i++)
    {
        int sum=0;
        for(int j=i;j<n;j++)
        {
            
        }
    }*/
}